/**
 * Payload collection slug for managed-client inquiries.
 * Run `npm run generate:types` after collection changes (output is gitignored).
 */

export const CLIENT_INQUIRIES_COLLECTION = "client-inquiries" as const;
