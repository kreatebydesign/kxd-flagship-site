/**
 * Multi-client Executive Memory stubs (Phase 32A).
 * Empty lenses — clients appear in the registry without inventing history.
 * Author items when relationship facts are clear.
 */

import type { ExecutiveMemoryItem } from "../types";

export const EMPTY_CLIENT_MEMORY: ExecutiveMemoryItem[] = [];
