/** Payload default cookie prefix — matches `cookiePrefix` in payload.config.ts */
export const PAYLOAD_AUTH_COOKIE_PREFIX = "payload";

export const PAYLOAD_ADMIN_LOGIN_PATH = "/admin/login";

/** Default post-login destination for KXD OS operations */
export const OPERATIONS_HOME_PATH = "/admin/operations/today";

/** Unified KXD OS launcher — Payload admin only */
export const OS_LAUNCHER_PATH = "/os";
