# App
NEXT_PUBLIC_SITE_URL=https://kreatebydesign.com

# Payload
PAYLOAD_SECRET=change-me-to-a-long-random-string
DATABASE_URI=postgres://postgres:password@127.0.0.1:5432/kxd
PAYLOAD_SQLITE_PATH=file:./.payload/kxd.sqlite

# Inquiries
KXD_INQUIRY_EMAIL=matt@kreatebydesign.com

# Stripe — server-owned integration boundary (Phase 37H / 37I / 37J)
# Names only. Never commit real keys. Presence is structural — not connectivity.
# Proposal checkout / webhook use these when set.
# Phase 37I/37J authorize deliberate test-mode connectivity, linking, and customer
# creation only when the secret key is test-mode. Live-mode keys remain rejected.
# Catalog, subscriptions, invoices, and payments remain closed.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Optional: browser Elements / future client UI (not required for current server checkout)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Analytics (browser tags — public)
NEXT_PUBLIC_GA4_MEASUREMENT_ID=
NEXT_PUBLIC_GTM_ID=
GOOGLE_SITE_VERIFICATION=

# Google Reporting (server-only — Phase 29C / 31B GA4 + Search Console)
# Production (Vercel): prefer Workload Identity Federation via Vercel OIDC (no JSON key).
# Do NOT reuse GOOGLE_CALENDAR_REFRESH_TOKEN — Calendar scopes are different.
# Per-client GA4 property ID + Search Console site URL live on Client Infrastructure.
# Per-client reporting entitlements (website-analytics, seo, …) live on Client Experience Profiles.enabledModules.
# See docs/REPORTING-PROVIDERS.md
#
# Production OIDC / WIF (all five required on Vercel Production):
GCP_PROJECT_ID=
GCP_PROJECT_NUMBER=
GCP_SERVICE_ACCOUNT_EMAIL=
GCP_WORKLOAD_IDENTITY_POOL_ID=
GCP_WORKLOAD_IDENTITY_POOL_PROVIDER_ID=
#
# Optional — protects /api/cron/reporting-ingest and cron bearer on admin ingest.
# CRON_SECRET=
#
# Local development fallbacks (used when not on Vercel):
GOOGLE_REPORTING_SERVICE_ACCOUNT_JSON=
# Aliases: GA4_SERVICE_ACCOUNT_JSON=  GSC_SERVICE_ACCOUNT_JSON=  GOOGLE_APPLICATION_CREDENTIALS=
# Optional OAuth alternative (reporting-specific refresh token with analytics.readonly + webmasters.readonly):
# GOOGLE_REPORTING_CLIENT_ID=
# GOOGLE_REPORTING_CLIENT_SECRET=
# GOOGLE_REPORTING_REFRESH_TOKEN=

# Google Ads reporting (Phase 32B — Shared Core provider id: ads; source: google-ads)
# Requires developer token + Ads-authorized credentials (adwords scope).
# Per-client customer ID (+ optional MCC login customer) live on Client Infrastructure.
# Do not enable google-ads entitlement until real ReportingFacts exist.
GOOGLE_ADS_DEVELOPER_TOKEN=
# OAuth refresh token used for Ads must include:
# https://www.googleapis.com/auth/adwords
# (Separate Ads token cache; GA4/GSC continue to use analytics + webmasters scopes.)

# Google Reviews — Places API
# Activate to pull live reviews into the homepage ReviewsSection.
# Without these, the site uses static fallback reviews (safe default).
#
# 1. Create a Google Cloud project and enable "Places API"
# 2. Generate an API key restricted to the Places API
# 3. Find your Place ID: https://developers.google.com/maps/documentation/javascript/examples/places-placeid-finder
#
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=
# Email / Resend
RESEND_API_KEY=
RESEND_FROM_EMAIL=Kreate by Design <hello@kreatebydesign.com>
# Optional — public portal URL for password reset links (defaults to https://portal.kreatebydesign.com in production)
PORTAL_PUBLIC_URL=https://portal.kreatebydesign.com

# Vercel Blob — Website Review / Website Workspace private attachments (Phase 18G)
# Production on Vercel requires durable blob storage (local filesystem is not writable).
# Either set BLOB_READ_WRITE_TOKEN, or rely on Vercel OIDC with BLOB_STORE_ID.
# Names only — never commit real token values.
# BLOB_READ_WRITE_TOKEN=
# BLOB_STORE_ID=